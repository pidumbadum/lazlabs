[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "lazlabs-core"
version = "0.1.0"
description = "Core business logic for Lazlabs School system"
requires-python = ">=3.11"
dependencies = []